# 前端优化阶段记录与未通过门禁

开始日期：2026-09-06；工作区复核日期：2026-09-08。

**状态：部分实现已落地；完整目标未完成，不具备“三道门禁全部通过”的交付条件。**

本任务依据 `docs/全栈开发提示词-ReAct团队协作版.md`，实际请求为优化前端，不扩大为改造后端或数据库。该文档中的资产、接口、安全、协作及视觉验收边界继续有效，未以单元测试代替浏览器截图或真实服务验证。

## 1. 本轮实际变更

以下清单只包含相对于本任务开始时工作区的新增改动，不把用户原有未提交代码算成本轮成果。

| 文件 | 本轮变更 | 影响与验证范围 |
| --- | --- | --- |
| `frontend/src/components/layout/AppHeader.vue` | 移动抽屉的初始焦点、双向 Tab 循环、Escape 回焦点、背景 inert 与滚动锁定；路由切换、宽屏断点、卸载时清理；主题/语言选中语义；44px 触控区 | 导航组件交互测试通过；真实视口排版尚未验收 |
| `frontend/src/views/MemoryBuilderView.vue` | 阻止空白标题和重复提交；处理 store 无返回值；失败保留草稿并聚焦错误；必填/错误关联；提交中禁用字段；减少动效时使用既有静态封面；表单 fieldset 重置、可收缩网格和手机按钮布局 | 创建流程组件测试通过；该文件原有的一项 TypeScript 错误消除；未证明真实后端创建成功 |
| `frontend/vite.config.ts` | 在真正生效的 injectManifest 配置中排除媒体库及大型地图、3D、图表、图谱、Mermaid vendor 预缓存 | 构建预缓存由 231 项 / 25,480.33 KiB 降为 83 项 / 4,034.57 KiB，体积减少约 84.2% |
| `frontend/tests/navigationAccessibility.spec.ts` | 新增 7 项实际组件交互测试 | 不依赖浏览器服务器，不属于视觉截图测试 |
| `frontend/tests/memoryBuilderFlow.spec.ts` | 新增 6 项创建流程回归测试 | 使用 mock store，覆盖成功、失败重试、重复提交、无效输入和空返回；不是后端 E2E |
| `docs/frontend-optimization-2026-09-06.md` | 保存变更、证据位置、回滚方法和待完成项 | 阶段记录，不是完整验收通过报告 |

预缓存变更没有删除或替换素材，也没有改变资源 URL。既有 service worker 对 script/style/worker 与 image/font 的运行时缓存规则仍保留。**未曾访问的大型功能和素材不再保证首次断网时可用**；不能把本次构建体积下降等同于已测得页面 LCP 或离线全功能可用。

## 2. 已取得的工程证据

原始证据保存在已被 Git 忽略的 `.codex-backups/frontend-20260906/qa/`，编译产物仍在被忽略的 `frontend/dist/`，未提交任何构建产物、日志或凭据。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 初始单元测试 | 14 个文件、92 项通过 | `unit-initial.log` |
| 当前单元测试 | 16 个文件、105 项通过 | `unit-current.log` |
| 新增导航与创建定向测试 | 13 项通过 | `navigation-builder-tests.log` |
| `npm run build` | 生产 bundle 与 service worker 构建完成 | `build-current.log` |
| `npm run typecheck` | 脚本退出 0，但不足以证明应用源码类型健康 | `typecheck-current.log` |
| `vue-tsc --noEmit -p tsconfig.app.json` | **失败：仍有 12 项原有错误，初始为 13 项** | `typecheck-app-initial.log`、`typecheck-app-current.log` |
| 资产完整性 | 基线登记的图片/视频 SHA-256 校验通过 | `.codex-backups/frontend-20260906/assets.sha256` |

根 `tsconfig.json` 使用 `files: []` 和项目 references。现有 `typecheck` 脚本未使用项目构建模式；直接指定应用配置揭示了普通脚本未报出的错误。因此，本报告不将“脚本返回 0”包装成“全前端无类型错误”。剩余错误位于原有的 `useInstancedMemory3d.ts`、`Memory3dViewer.vue`、`AdminEntryView.vue`、`AdminHomeView.vue`、`AdminResonanceManagementView.vue`，本轮未顺带修改这些文件。

构建仍有既存的大 chunk 警告；创建组件测试暴露既存本地化文案含 `<br/>` 的 vue-i18n 警告。二者均未被隐藏或宣称已解决。

## 3. 视觉走查：0 / 12 屏，未通过

尚未取得任何本轮真实浏览器截图，不能对“无重叠、无溢出、文字不截断、艺术质感”给出通过结论，也不能提供可信的改造前后视觉评分。

| 核心视图 | 桌面 1440 × 900 | 手机 390 × 844 |
| --- | --- | --- |
| 登录 | 未截取 / 未审批 | 未截取 / 未审批 |
| 记忆列表 | 未截取 / 未审批 | 未截取 / 未审批 |
| 记忆详情 | 未截取 / 未审批 | 未截取 / 未审批 |
| 记忆构建器 | 未截取 / 未审批 | 未截取 / 未审批 |
| 灵魂共鸣大厅 | 未截取 / 未审批 | 未截取 / 未审批 |
| 聊天会话 | 未截取 / 未审批 | 未截取 / 未审批 |

已有代码的布局目标是可收缩的双栏、手机下单栏表单、44–48px 操作区、保留暗色桃粉淡紫主题及已有字体/素材。它们只是代码层面的目标，不替代多模态验收。

## 4. 后端与链路证据：未执行，不宣称安全门通过

本轮未启动或修改微服务、Docker 工程、数据库，也未运行会注册用户、创建记忆或发送消息的真实业务脚本。

| 验证项 | 要求的真实证据 | 本轮状态 |
| --- | --- | --- |
| Maven 与应用上下文 | 各服务编译、Bean 和上下文加载结果 | 未执行 |
| Docker 健康 | 约定中间件、微服务及前端容器 healthy / UP | 未执行 |
| 页面资源与 API 健康 | 操作走查无新增 404 / 未捕获 5xx | 未执行；本地文件哈希不能证明 HTTP 健康 |
| 完整业务链路 | 注册 → 登录 → 创建 → 列表 → 详情 → 共鸣 → 实时聊天 | 未执行；105 项测试不能替代此项 |
| 非所有者读取私密记忆 | 明确拒绝的 HTTP 报文 | 未执行 |
| 非所有者修改、删除、锁定公开/私密记忆 | 逐操作 403 检查矩阵 | 未执行 |
| 管理端鉴权 | 匿名 401，普通用户 403 | 未执行 |
| 伪造身份头 | 公共请求伪造身份不被接受的报文 | 未执行 |
| 错误脱敏与语义 | 规范 JSON、无 SQL / Java 堆栈、正确状态码 | 未执行 |

仓库已有 `scripts/backend_e2e_idor_verification.py`，但“有脚本”不是“已通过”。其脱敏步骤允许不存在资源返回 404 或 500，且它没有覆盖浏览器实时聊天的完整闭环，恢复验证时需要补强，不能直接视为提示词全部安全要求的证明。

另需审查原有 service worker 的认证 API GET 缓存是否跨账号隔离；本轮未验证或修改该策略，不能据此宣称已完成安全审计。

## 5. 仍需完成的原始范围

- 登录首屏与手机版表单顺序、自动背景切换对减少动效/后台页面的遵从，仍需实现并实测。
- 记忆列表的主操作层级、搜索无结果与无收藏空态区分，仍需实现并实测。
- 记忆详情的完整长文阅读、长标题及移动操作布局，仍需实现并实测。
- 共鸣大厅的未搜索/加载/错误/无匹配状态、切换基准记忆后的旧结果隔离、重复视频开销，仍需实现并实测。
- 聊天的窄屏输入区、长消息、快速切换会话的异步结果隔离、中文输入法 Enter 行为，仍需实现并实测。
- 已修改的导航和构建器仍须接受独立代码审查及真实双端视觉审批；定向测试不是替代品。
- 三道完整审批门与最终证据库仍待补齐，未缩减为“这次通过的 105 项测试”。

## 6. 阻塞与协作记录

前端服务器命令 `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort` 在沙箱内因 `listen EPERM` 失败。按规则申请升级后，自动审批基础设施返回模型不可用的 404 并拒绝执行。已向用户询问明确许可；未通过其他工具、端口或间接启动方式绕过该拒绝。

多 Agent 已按无重叠写集合分派。导航和构建器子任务留下了实际补丁，其后响应流中断；这些补丁由主控逐项核对并以组件测试验证。其他子任务遇到限流/传输失败，没有将“已派发”记为“已完成”，也没有伪造独立视觉审批结论。重复失败后不再无限重试同一路径。

恢复完整工作需要：允许仅本机监听的预览服务器、可用的子任务/审批服务，以及进行真实后端验证所需的可用测试环境。服务器许可本身不等于已经取得截图或后端安全证据。

## 7. 回滚锚点：保留任务开始前的未提交改动

任务起点 Git HEAD 为 `3418fa192c9affe3c82f04f86c562f6b45a11f79`，但工作区当时已有大量用户改动，**不能用 `git reset --hard`、`git restore` 或直接 checkout HEAD 作为本轮回滚方法**。

本地证据目录 `.codex-backups/frontend-20260906/` 中：

- `before.tar.gz`：任务开始时的前端源码、测试及涉及的配置快照，包含原有未提交工作。
- `preexisting.patch`：任务开始时前端已有的 tracked diff。
- `baseline/`：上述快照的展开目录。
- `task-only.patch`：相对于该快照的本轮差异，不包含原有用户修改。
- `task-files.txt`：本轮变更文件列表。

需要回滚时，在仓库根目录先检查，确认通过后再执行第二条：

```bash
git apply --reverse --check .codex-backups/frontend-20260906/task-only.patch
git apply --reverse .codex-backups/frontend-20260906/task-only.patch
```

本任务只执行第一条无写入的检查，不擅自回滚。若任务结束后这些文件又有修改，检查可能失败，此时应停止并人工合并，不能加 force 覆盖后续工作。备份和补丁都保存在本机忽略目录，清理该目录前应另行保存。
